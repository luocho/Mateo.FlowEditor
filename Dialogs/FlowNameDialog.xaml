<Window x:Class="FlowEditor.Dialogs.FlowNameDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="380" Height="190" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="流程名称" FontWeight="SemiBold"/>
        <TextBox x:Name="FlowNameBox" Grid.Row="1" Margin="0,7,0,0"
                 TextChanged="FlowNameBox_TextChanged"/>
        <TextBlock x:Name="ValidationText" Grid.Row="2" Margin="0,5,0,0"
                   Foreground="#FFE53935" FontSize="12"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="取消" Width="76" IsCancel="True" Margin="0,0,8,0"/>
            <Button x:Name="ConfirmButton" Content="确定" Width="76" IsDefault="True"
                    Click="ConfirmButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
